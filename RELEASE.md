# v1.3.14 — 服务层重构 + 回归安全网

> 2026-06-30

---

## 代码结构

- 抽出 `config.py`、`db.py`、`services/attendance.py`、`services/records.py`，降低 `app.py` 的业务逻辑体积
- 出库历史、留存记录、今日记录、调试记录和考勤逻辑逐步收口到 service 层
- 新增 `scripts/inspect_db.py`，方便排查服务器 SQLite 数据和缺失日期

## 回归保护

- 新增 `tests/test_regressions.py`，覆盖跨日期出库保护、考勤历史编辑、留存负数保护、今日记录接口和留存列表接口
- 正式版本更新到 `v1.3.14`，刷新 Docker/PWA 静态资源版本

---
# v1.3.13 — 考勤历史编辑

> 2026-06-29

---

## 🕐 考勤记录

- `查看全部` 页面支持编辑旧考勤记录
- 可修改日期、开始时间、结束时间和备注，工时随时间自动重算
- 全部记录页补充删除入口，旧记录维护更完整
- 静态资源版本更新到 `1.3.13`，确保手机端刷新到新样式和脚本

---

# v1.3.12 — 出库日期保护 + 正式发布

> 2026-06-29

---

## 🛡 出库记录保护

- 修复切换日期后旧 `record_id` 残留，可能把数据保存到错误日期的问题
- 日期覆盖只在设置当天有效，避免手机端 `localStorage` 跨天沿用旧日期
- 新日期首次保存强制完整保存，避免用增量数据创建缺项记录
- 后端拒绝 `record_id` 与 `record_date` 不一致的跨日期增量保存
- 数据库启动时合并同日重复记录，并为 `records.record_date` 建立唯一索引

## 🔧 部署与缓存

- `APP_VERSION` 支持从 Docker 环境变量读取
- Docker/Compose 版本号更新到 `1.3.12`，用于刷新 PWA 静态资源缓存

---

# v1.3.5 — 考勤打卡 + 保存重试 + 备份

> 2026-06-07

---

## 🆕 考勤打卡

- 新增 🕐 考勤标签页，记录每日工作时间
- 自动取整当前时间 + 预选 2 小时
- 多段时段编辑（+ 添加时段），导出时同日期自动合并
- 一键请假按钮
- 最近 3 天记录显示，全部记录二级页面
- **Excel 导出**：选择起止日期（默认 30 天），格式对齐模板，列宽/加粗/行高/对齐全部保留

## 🛡 保存重试

- 网络异常时自动重试 3 次（间隔 1s → 3s → 6s）
- 重试期间 header 渐进变红，状态栏显示 `🔄 重试中 (1/3)...`
- 3 次全失败后 header 全红 + Toast 弹窗 + 红色横幅三重提醒
- 网络恢复后自动清除错误状态

## 🔧 其他

- **备份脚本** `backup.sh`：sqlite3 .backup 正确处理 WAL，每天凌晨 3 点自动备份，保留 7 天自动轮转
- **离线模拟**：三连击版本号 → Dev Tools → 勾选 📴 模拟离线
- **联动开关**：留存页可关闭与出库的联动，关闭时需二次确认
- **出库为 0 时 + 留存**：弹确认窗，本次访问只提示一次
- **标签切换记忆**：从历史页返回自动恢复上次所在标签

---

## 📦 部署

```bash
git clone https://github.com/oHapp/EggNum.git
cd EggNum
cp .env.example .env
docker compose up -d --build
```
